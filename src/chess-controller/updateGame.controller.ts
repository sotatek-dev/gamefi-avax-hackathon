// const User = require('../models/user')

// export async function updateGame(address, amount) {
//   await User.create({
//     address: address, 
//     balance: amount, 
//     amount: amount, 
//     message_player_1: message, 
//     signature_player_1: signature
//   });
//     await db.query(
//         `INSERT INTO game_pn (address, amount) VALUES (${address}, ${amount})`
//       );
//     return {'success': true, status: 200, msg: 'Insert game_pn successfully'}
// }